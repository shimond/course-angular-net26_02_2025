// import { computed, importProvidersFrom } from "@angular/core";
// import { PeopleSearchStore } from '../people-search.store';

// export function ComputSearch(store: any) {
//     return computed(() => store.results().filter(p => p.name.toLowerCase().includes(store.query().toLowerCase())))
// }


